/**
 * @file src/config/constants.ts
 */



/** 
 * @TODO move this to json. read in with a function. 
 * List of strings from VendorCSV['Vendor'] that correspond to humans so know when to set the radio field `isperson`=`"T"` in NetSuite Vendor Record 
 * */
export const HUMAN_VENDORS_ORIGINAL_TEXT: string[] = [
    'Andrew Garwood',
]